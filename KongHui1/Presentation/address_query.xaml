<Page
    x:Class="KongHui1.Presentation.address_query"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KongHui1.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <Page.Resources>
    <!-- Modern TextBox 样式 -->
    <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
      <Setter Property="Background" Value="White"/>
      <Setter Property="Foreground" Value="Black"/>
      <Setter Property="BorderBrush" Value="#0078D7"/>
      <Setter Property="BorderThickness" Value="2"/>
      <Setter Property="CornerRadius" Value="5"/>
      <Setter Property="Padding" Value="10"/>
      <Setter Property="FontSize" Value="16"/>
    </Style>

    <!-- 按钮样式 -->
    <Style x:Key="ModernButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="#0078D7"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="CornerRadius" Value="5"/>
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="Padding" Value="10"/>
    </Style>
  </Page.Resources>
  
  <Grid  Margin="10">
    <!-- 页面头部 -->
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,0">
      <Image Source="ms-appx:///Images/BackIcon.png" Width="25" Height="25" PointerPressed="BackButton_Click"/>
      <TextBlock Text="地址查询" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0,0,0" Foreground="#333"/>
    </StackPanel>
  

  <!-- 搜索区域 -->
  <Grid VerticalAlignment="Top" Margin="0, 60, 10, 10">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="3*" />
      <ColumnDefinition Width="2*" />
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- 左侧：搜索区域 -->
    <StackPanel Margin="10" Grid.Column="0" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
      <TextBox x:Name="SearchTextBox" PlaceholderText="请输入关键词" Width="300" Height="40" Margin="0,0,20,10" />
      <Button Content="搜索" Style="{StaticResource ModernButtonStyle}" Click="SearchButton_Click"/>
        <Button Content="重置" Style="{StaticResource ModernButtonStyle}" Click="SearchButton_Click2"/>
      </StackPanel>

    <!-- 地址展示区域 -->
      <ListView x:Name="DataListView" Grid.Column="0" Grid.Row="1" Margin="10,10,0,0">
        <ListView.ItemTemplate>
          <DataTemplate x:DataType="local:AddressData">
            <Border Width="500" Height="120" Background="White" CornerRadius="10" BorderBrush="#ddd" BorderThickness="1">
              <StackPanel Margin="10">
                <!-- 站点名称 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                  <TextBlock Text="站点名称: " FontWeight="Bold" Foreground="#333" FontSize="18" Width="100"/>
                  <TextBlock Text="{Binding siteName}" Foreground="#555" FontSize="16"/>
                </StackPanel>

                <!-- 联系电话 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                  <TextBlock Text="联系电话: " FontWeight="Bold" Foreground="#333" FontSize="18" Width="100"/>
                  <TextBlock Text="{Binding phoneNumber}" Foreground="#555" FontSize="16"/>
                </StackPanel>

                <!-- 地址 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                  <TextBlock Text="地址: " FontWeight="Bold" Foreground="#333" FontSize="18" Width="100"/>
                  <TextBlock Text="{Binding address}" Foreground="#777" FontSize="16"/>
                </StackPanel>
              </StackPanel>


            </Border>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>

    </Grid>
  </Grid>
</Page>
